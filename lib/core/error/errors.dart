class NetworkException implements Exception {
  // NetworkExceptionException(this.message);
  // final String message;
}

class BadRequestException implements Exception {
  // NetworkExceptionException(this.message);
  // final String message;
}
